# Frontend environment variables
# Copy this file to .env and fill in your values.

# Backend base URL used by the app (leave empty to use the Vite proxy)
VITE_API_BASE=""

# Proxy target for the Vite dev server (avoids CORS during development)
VITE_API_PROXY_TARGET=""
